# 闭环工作台快速手册

路径：`/workbench`

## 三步完成闭环
1. 选择 Profile 并执行隔离检查。
2. 创建闭环 Run（默认 TikTok 模板）。
3. 执行闭环并跳转 Live 查看结论卡片。

## 适用场景
- 新同学快速上手，不用跨页面拼流程。
- 回归验证时一键执行标准闭环。

## 结果判断
- Live 中“结论卡片”显示断言信息与隔离报告状态。
- 如失败可直接“一键重跑”。
